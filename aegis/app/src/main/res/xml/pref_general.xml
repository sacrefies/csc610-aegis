<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
     dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!-- This needs to change to a   <MultiSelectListPreference/> but
         but keep getting a Binary XML file line #6: Error inflating class java.lang.reflect.Constructor error-->

    <ListPreference
        android:defaultValue="@array/empty_array"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:key="example_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_display_crimes" />

    <ListPreference
        android:defaultValue="@array/empty_array"
        android:entries="@array/pref_example_list_titles_dates"
        android:entryValues="@array/pref_example_list_values_dates"
        android:key="example_dates"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_display_crimes_dates" />

</PreferenceScreen>
